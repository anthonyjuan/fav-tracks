const initialState = [
  { id: 1, title: 'how to learn react', url: 'lorem ipsum', img: 'lorem' },
];


const tracksReducer = (state = initialState, action) => (state);

export default tracksReducer;
